# GRAPH 模块开发准入评审单

> 文档编号：NAV-006
> 版本：V1.0
> 创建日期：2026-03-21
> 最后更新：2026-03-21
> 维护人：项目负责人
> 状态：已冻结（首轮骨架开发基线）
> 用途：记录 GRAPH 模块首轮骨架开发准入结论
> 变更方式：后续修改需通过新一轮准入评审

---

# GRAPH 模块开发准入复核结论

## 一、复核范围
- 模块编号：`GRAPH`
- 模块名称：记忆与图谱引擎
- 依据文档：
  - `STR-002`《项目需求总纲》
  - `NAV-004`《KB+INGEST 模块级开发准入评审单》
  - `NAV-005`《TWIN 模块级开发准入评审单》
  - `GRAPH-001`《图谱记忆层设计》
  - `GRAPH-002`《学生时序记忆模型》
  - `GRAPH-003`《事实关系事件规范》
  - `GRAPH-004`《记忆增量更新机制》
  - `GRAPH-005`《GraphRAG 检索设计》
  - `GRAPH-008`《知识图谱实体关系定义》
  - `GRAPH-009`《图谱写入与版本回滚》
  - `GRAPH-010`《图谱质量评估指标》
  - `GRAPH-011`《GRAPH 首轮切片定义》
  - `ARCH-025`《TWIN-GRAPH 写入契约》
  - `GRAPH-012`《GRAPH 对象写入与更新映射表》
  - `ARCH-026`《GRAPH 最小验收矩阵》
  - `ARCH-027`《GRAPH 端到端样例集》
  - `OPS-011`《GRAPH 试点范围与责任边界》

## 二、当前已具备的开发条件
1. GRAPH 的首轮切片已经冻结到安徽 / 高中 / 高一 / 物理 / `PHY_PEP_G1_V1`，输入源只允许家长文本、老师反馈文本、单页扫描/OCR。
2. TWIN → GRAPH 写入契约已经明确，Fact / Relation / Event / Snapshot / Retrieval Unit 的来源、降级规则与责任边界已经收口。
3. 对象写入与更新映射、最小验收矩阵、端到端样例与试点范围边界已经补齐，足以支撑首轮骨架开发准入判断。

## 三、当前仍缺失的关键条件
1. 无阻断性缺失。
2. 剩余事项均属于实现期、联调期和试点执行期，不再属于准入前置条件。
3. 任何超出当前冻结范围的扩圈需求，必须重新走模块级准入评审。

## 四、按开发准入检查表逐项判断
1. 模块职责是否明确：是
2. 输入输出是否明确：是
3. 核心对象是否已定义：是
4. 数据流位置是否明确：是
5. 下位文档是否齐备：是
6. 架构组件是否已映射：是
7. 验收对象是否明确：是
8. 场景与输出是否闭环：是
9. 责任边界是否冻结：是

## 五、开发准入结论
- 允许

## 六、若为“允许”，必须明确
- 允许进入什么范围的开发：
  - 允许 GRAPH 进入首轮骨架开发。
  - 允许范围冻结为：安徽 / 高中 / 高一 / 物理 / `PHY_PEP_G1_V1`。
  - 允许输入源仅限：家长文本、老师反馈文本、单页扫描/OCR。
  - 允许建设单学生图谱骨架、Fact / Relation / Event / Snapshot / Retrieval Unit 的最小写入闭环、对象级版本链与基础回滚、最小 GraphRAG 检索闭环。
- 不允许进入什么范围的开发：
  - 不允许进入全量实现。
  - 不允许进入全量联调。
  - 不允许扩到多地区、多学科、多教材版本及扩展输入源。
  - 不允许将 OBS 的展示职责提前塞入 GRAPH。
  - 不允许将 SIM 的推演职责提前塞入 GRAPH。
  - 不允许直接固化原始长文本、低置信 OCR 候选或下游产品化文案。

## 七、若不是“允许”，必须给出最小补齐清单
- 不适用。本轮结论为“允许”。

## 八、下一步建议
- 立即动作：
  - 按 `GRAPH-011 / ARCH-025 / GRAPH-012` 直接拆 GRAPH 首轮骨架开发任务。
  - 以 `ARCH-026 / ARCH-027 / OPS-011` 作为实现期验收、回归和试点边界口径。
- 后续动作：
  - 在真实试点数据下验证样本覆盖、异常分布和回滚稳定性。
  - 若要扩展到其他地区、学科、教材版本或输入源，必须重新发起 GRAPH 模块准入评审。

---

## 附：6 位评委第二轮共识

### 共识点

1. 上游 KB+INGEST 与 TWIN 已冻结，GRAPH 的输入边界稳定。
2. 首轮切片、写入契约、对象映射、验收矩阵、样例集和试点边界已经补齐。
3. GRAPH 当前可以正式进入首轮骨架开发，但仅限冻结范围。

### 分歧点

无实质性分歧。

### 最终结论

在当前冻结范围内，GRAPH 已经从“条件允许”推进到“允许”。
