# 2026-03-27 Bot 进度日报

**日期**: 2026-03-27
**keywords**: bot 进度日报 dev-bot monitor thread tmux preflight task thread monitor session tbot qwen3.5-plus

---

## 概述

今天完成的前置基建收口有四项：

1. 接管并核对 2026-03-27 当天 `workbot` 运行现场，确认当前唯一前台 tmux 会话为 `tbot`
2. 确认当前用户线程作为当天 task thread 真源，并新建当天 monitor thread
3. 修正 tmux 指挥链中的监控分流逻辑，并验证门铃链路能够稳定投递到当前注入的唯一 `CODEX_THREAD_ID` 对应监控 app thread
4. 补齐今天的事实层日志，记录 task / monitor 线程与 tmux 运行面的留证信息

---

## 今日线程与会话真源

### Task Thread

- 类型：当天任务线程
- 线程 ID：`019d2e72-d706-71e3-8f79-742ad13f611e`
- 说明：即当前指挥官接管并继续推进的用户线程

### Monitor Thread

- 类型：当天监控线程
- 线程 ID：`019d2e90-4285-7373-a0a4-9997d8fa3ea5`
- 创建方式：`codex exec --json -C /Users/busiji/workbot ...`
- 就绪回执：`MONITOR_THREAD_READY`

### Task tmux Session

- 会话名：`tbot`
- 当前 window id：`@0`
- 当前 window title：`dev-bot`
- 当前 pane id：`%0`
- 当前 pane title：`dev-bot`
- 当前 cwd：`/Users/busiji/workbot`
- 当前运行命令：`node`

### Monitor tmux Session

- 会话名：`workbot-watch-2026-03-27`
- 当前 pane id：`%2`
- 当前 pane title：`monitor-bot`
- 当前 cwd：`/Users/busiji/workbot`
- 当前运行命令：`bash`
- 观察目标：`%0`
- 事件日志：`/Users/busiji/workbot/workspace/artifacts/tmux-look/2026-03-27-monitor-events.jsonl`

---

## 今日前置核查结论

- 决议文件要求的“task thread / monitor thread / task tmux session / monitor tmux session”四件套中，接管前只存在 task tmux 面雏形
- 接管前存在的问题：
  - 没有 2026-03-27 日报
  - 没有当天 monitor thread 留证
  - `dev-bot` pane title 曾漂移，不满足 pane title 必须等于 bot 名的硬规则
- `tmux-fmd` 仍写死旧线程 ID，监控告警会打向过期 monitor app thread
- 接管后已完成：
  - 确认 task thread 与 monitor thread 的当天真源
  - 修正 `scripts/tmux-fmd.py`，门铃目标统一收口为启动时注入的 `CODEX_THREAD_ID`
  - 记录今天的 task / monitor 事实入口

---

## 今日代码级基建修正

### `scripts/tmux-fmd.py`

- `resolve_codex_thread_id()` 作为唯一线程解析入口继续保留
- 门铃真源收口为当前环境的 `CODEX_THREAD_ID`
- `WORKBOT_MONITOR_THREAD_ID` 与 `DEFAULT_CODEX_THREAD_ID` 不再视为正式门铃真源

> **纠偏说明**：这里保留了之前的文字，但须指出它描述的是旧多来源逻辑。当前运行规范已经废弃 `WORKBOT_MONITOR_THREAD_ID` 与 `DEFAULT_CODEX_THREAD_ID`，门铃目标只靠启动该运行面注入的单一 `CODEX_THREAD_ID`。任何文档或脚本中提到其它变量都应被视为历史痕迹，后续必须清理或重写。

> **2026-03-30 追加纠偏**：这里的 `CODEX_THREAD_ID` 语义已经进一步收口为 Codex app thread id，不允许再被理解成 CLI session id。tmux handoff 的 delivery 也已从旧的本地 CLI 恢复方式切到常驻 window IPC bridge；watcher 负责看和落队列，delivery runner 负责确保 bridge 常驻，真正投递以 owner window 的成功响应为验收。

这意味着从今天起，谁负责接收 tmux 门铃，不再由脚本内部多来源回退决定，而由启动该运行面的 `CODEX_THREAD_ID` 显式决定。

> **2026-03-30 追加纠偏**：上述 2026-03-27 口径只解决了“门铃目标唯一化”，但没有把 delivery 语义明确为当前窗口可见。当时的表述仍可能让人误读为“只要写入 `CODEX_THREAD_ID`，本地 CLI session resume 就等于投递成功”。当前正式口径已经更新为：`CODEX_THREAD_ID` 必须是 Codex app thread id；tmux handoff 通过常驻 window IPC bridge 进入目标 thread 的 owner 窗口；不再以 `codex exec resume` 作为最终投递方式。

---

## 当日风险与未收口项

- 当前 Terminal 前台只看到一个 `tbot` 会话，monitor tmux 前台面仍需补建并验证
- 当前 `dev-bot` pane 已在 Claude 聊天界面，但需要重新注入标准 `dev-bot` 身份并复查消息已经真正进入聊天流
- 当前 `dev-bot` 身份内容已重新注入，下一步是正式发第一条任务并复查响应
- 仓库当前是脏树，存在大量历史删除与未跟踪文件；今天接管不负责清场，只在最小范围内补前置基建与指挥链

---

## 当日结论

- 今天的前置基建已从“口头说完成”推进到“线程与日志有真源、监控分流可落地”
- monitor tmux 面已经补齐，并完成了对 `%0` 的首次有效观测
- 在此基础上，指挥官可继续向 `dev-bot` 正式下发任务
- 已向 `%0` 的 `dev-bot` 正式下发首条任务：在 AEdu 第一阶段最小闭环范围内执行“实现核对 + 最小验证 + 仅在发现真实缺口时补代码”
- 当前 `dev-bot` 已进入 `Deliberating`，监控面正在持续观察并写入事件日志

---

## 证据来源

- `/Users/busiji/workbot/workspace/memory/kb/decisions/2026-03-25-workbot-project-agents-and-runtime-surfaces.md`
- `/Users/busiji/workbot/workspace/memory/kb/decisions/2026-03-25-ce-task-governance.md`
- `/Users/busiji/workbot/scripts/tmux-fmd.py`
- `tmux list-sessions -F '#{session_name} attached=#{session_attached} windows=#{session_windows} created=#{t:session_created}'`
- `tmux list-windows -a -F '#{session_name} #{window_index} #{window_id} #{window_name} active=#{window_active}'`
- `tmux list-panes -a -F '#{session_name} #{window_id} #{window_name} #{pane_id} #{pane_title} #{pane_current_command} #{pane_current_path} active=#{pane_active}'`
- `codex exec --json -C /Users/busiji/workbot ...`
