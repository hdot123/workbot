# 2026-04-02 Bot 进度日报

**日期**: 2026-04-02
**keywords**: AEdu GitLab CE 生命周期 状态流转 dev_done qa_done doc_synced ce_synced

---

## 概述

今天围绕 AEdu 的任务状态流转完成了三件事：

1. 把本地 `dev / qa / doc` 三张 task list 统一到同一条状态链
2. 执行了一轮 GitLab CE 正式状态纠偏评论，把“待开发 / 待启动”拉回到当前真实进度
3. 发现并纠正了“开发完成即关闭 CE issue”这一错误流程，并把正确规则沉淀进记忆层

---

## 今日结论

- AEdu 的一张 CE 正式任务单必须贯穿 `dev -> qa -> doc -> ce -> done` 全生命周期
- `dev_done` 不允许关单
- `qa_done` 不允许关单
- 只有 `done` 才允许关单
- QA、Doc、Commander 后续进度必须继续写回同一张 CE issue

---

## 今日本地状态文件调整

- 已重写 `/Users/busiji/workbot/workspace/projects/AEdu/dev-task-list.md`
- 已重写 `/Users/busiji/workbot/workspace/projects/AEdu/qa-task-list.md`
- 已维护 `/Users/busiji/workbot/workspace/projects/AEdu/doc-task-list.md`
- 已新增 `/Users/busiji/workbot/workspace/projects/AEdu/ce-sync-plan.md`

这些文件当前共同承载本地状态链：

`todo -> in_progress -> blocked -> dev_done -> qa_done -> doc_synced -> ce_synced -> done`

---

## 今日 CE 实际更新

已向 `aedu/workbot` 的以下 issue 写入 `[Audit sync 2026-04-02]` 评论：

- `#28`
- `#30`
- `#33`
- `#37-69`

本轮动作性质：

- 只做状态纠偏
- 不做关单
- 不给 bot 新建正式任务
- 不打断后续 QA / Doc 在同一 issue 上续写进度

---

## 今日纠偏点

### 错误流程

- 开发完成后就关闭 CE issue
- 结果导致 QA、Doc 后续没有同一正式任务可继续续写

### 纠偏后流程

- `dev_done`: issue 继续 `opened`
- `qa_done`: issue 继续 `opened`
- `doc_synced`: issue 继续 `opened`
- `ce_synced`: issue 已完成本轮同步动作
- `done`: issue 才允许关闭

---

## 今日正式记忆沉淀

- 新增决策：
  - `/Users/busiji/workbot/workspace/memory/kb/decisions/2026-04-02-aedu-ce-lifecycle-rule.md`
  - `/Users/busiji/workbot/workspace/memory/kb/decisions/2026-04-02-aedu-local-repo-first-and-handoff-order.md`

---

## tmux 执行提醒纠偏

- `lookme` 当前抓 tmux 最近 `5` 行并按 hash 检测重复状态
- `accept edits on ...` 被明确纠偏为 Claude 界面提示，不属于审批 SOP
- 若最近 `5` 行连续两轮 hash 相同且仍停在该提示附近，应输出“去 pane 窗口继续执行当前任务”的执行提醒
- 只有真实 `approval_prompt` / `approval_stuck` 才继续使用“去 `{target}` 检查 SOP 状态”的审批提醒
- 一旦出现 pane 执行提醒，口径应视为强执行信号，必须立刻去对应 pane 处理

这条决策用于约束后续 AEdu 的 CE 生命周期写法，防止次日或后续执行再次回到“开发完成就关单”的错误模式。

今天还追加固定了一条执行顺序规则：

- 当前 GitHub 上的版本视为正常基线
- 后续任务一律以最新本地仓库为直接执行真源
- 旧口径（已废弃）：曾写成 `dev -> qa -> doc -> rea-bot -> codex submit/push`
- 旧口径（已废弃）：曾把 `rea-bot` 仅理解为流程末尾最终审计位
- 旧口径（已废弃）：曾把 `codex submit/push` 写成只在 `rea-bot` 之后执行的单一路径

2026-04-02 追加纠偏：

- 首轮审计入口已改为固定由 `rea-bot` 先完成
- 新顺序以决策文件为准：`rea-bot(首轮审计) -> dev -> qa -> doc -> codex submit/push`
- 如需最终收口复核，可再次进入 `rea-bot`，但不再把 `rea-bot` 只放在流程尾部理解

今天还新增固化了一条指挥官派单规则：

- 用户给 AEdu 任务后，先查本地四张 task-list，再查 GitLab CE API，再按阶段和 `write_scope` 分配 bot
- CE 正式收口仍由指挥官自己处理，不下放给 bot
- OCR 正式方案统一按 `百度 OCR API only`

---

## 后续动作

- 先执行正式 QA
- 再同步项目文档与验收材料
- 然后在同一批 CE issue 上续写 `qa_done`、`doc_synced`
- 等任务真正达到 `done` 后，再执行关闭
## 2026-04-02 CE Sync And Next-Round Intake

- Commander 已通过 GitLab CE API 完成 AEdu 阶段二正式 CE 同步。
- 保持 `opened` 的正式 issue：`#28`、`#30`、`#31`、`#33`、`#56`。
- 已评论并关闭：`#34-36`、`#37-55`、`#57-69`，`#51` 已按验收材料关单。
- `#56` 继续保留为唯一残余阻塞：本地 OCR runner backend 未接通。
- 本地任务表已登记阶段三观察层增强期下一轮任务：
  - DEV：`DEV-008` ~ `DEV-010`
  - QA：`QA-006` ~ `QA-008`
  - DOC：`DOC-009` ~ `DOC-010`
  - REA：`REA-005` ~ `REA-006`
